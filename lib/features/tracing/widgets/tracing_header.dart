import 'package:flutter/material.dart';

class TracingHeader extends StatelessWidget {
  const TracingHeader({super.key});

  @override
  Widget build(BuildContext context) {
    return const Row(
      mainAxisAlignment: MainAxisAlignment.spaceBetween,
      children: [
        // Back button
        // AppBackButton(),
      ],
    );
  }
}
